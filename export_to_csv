#!/usr/bin/env python3

import csv
import sys
import stem.control

# Devido à versão da biblioteca 'stem' no Tails, as classes estão organizadas assim:
try:
    # 1. Tenta a forma de importação mais provável no Debian/Tails:
    from stem.control import Controller # Classe de Conexão (era ControlPort)
    from stem import CircStatus, InvalidRequest # Classes de Status e Exceção
    
except ImportError:
    # Fallback caso a organização das classes seja diferente:
    try:
        from stem import CircStatus, Controller, InvalidRequest
    except ImportError:
        print("\n❌ ERRO FATAL DE IMPORTAÇÃO: A biblioteca 'stem' não pode ser acessada.")
        print("Certifique-se de que 'python3-stem' está instalado via APT.")
        sys.exit(1)


# Caminho onde o arquivo CSV será salvo (Usando /tmp para evitar problemas de permissão)
csv_file_path = "/tmp/circuitos_tor.csv" 
control_port = None # Inicializa a variável para garantir que ela exista no 'finally'

print("Tentando conectar à porta de controle do Tor...")

try:
    # Solução do erro 'missing required positional argument':
    # Conecta explicitamente à porta padrão 9051 e autentica (necessário na sua versão).
    control_port = Controller.from_port(port=9051)
    control_port.authenticate() 
    
    circuit_data = []
    
    # Itera sobre os circuitos (o restante da lógica é o seu código original)
    for circuit in control_port.get_circuits():
        
        if circuit.status == CircStatus.BUILT:
            path_str = " -> ".join(
                f"{entry.nick} ({entry.fingerprint})" for entry in circuit.path
            )
            
            circuit_info = {
                "id": circuit.id,
                "status": circuit.status.name, 
                "path": path_str,
                "purpose": circuit.purpose.name, 
            }
            circuit_data.append(circuit_info)

    # Escreve os dados no CSV
    if circuit_data:
        keys = circuit_data[0].keys()
        with open(csv_file_path, 'w', newline='', encoding='utf-8') as output_file:
            dict_writer = csv.DictWriter(output_file, fieldnames=keys)
            dict_writer.writeheader()
            dict_writer.writerows(circuit_data)
        
        print(f"\n🎉 SUCESSO! Circuitos exportados para: {csv_file_path}")
        print(f"Total de circuitos exportados: {len(circuit_data)}")
    else:
        print("\n⚠️ NENHUM circuito Tor 'Built' (construído) ativo foi encontrado.")

# Tratamento de Erros
except InvalidRequest as exc: 
    print(f"\n❌ ERRO DE COMUNICAÇÃO (InvalidRequest).")
    print("Verifique se o Tor está completamente conectado e rodando.")
    print(f"Mensagem: {exc}")
except ConnectionRefusedError:
    print(f"\n❌ ERRO DE CONEXÃO. O script não conseguiu se conectar à porta de controle do Tor (9051).")
    print("No Tails, inicie/reinicie o Tor Browser e tente novamente.")
except Exception as e:
    print(f"\n❌ Ocorreu um erro inesperado: {e}")
    
finally:
    # Garante que a conexão seja fechada, mesmo se ocorrer um erro
    if control_port:
        control_port.close()
